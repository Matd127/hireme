import { LoaderWrapper } from "./LoaderStyle";
import { Ball } from "./LoaderStyle";

const Loader = () => (
    <LoaderWrapper>
      <Ball />
      <Ball />
      <Ball />
    </LoaderWrapper>
  );
  
  export default Loader;